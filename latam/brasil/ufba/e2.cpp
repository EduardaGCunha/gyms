#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(0); cin.tie(0)
#define int long long
#define endl '\n'


int d1[] = {0, 0, 1, 1};
int d2[] = {0, 1, 0, 1};

int vertical(int vx, int k){
    
}


signed main(){
    fastio;
    int ax, ay, bx, by; cin >> ax >> ay >> bx >> by;

    int ans = abs(ax-bx) + abs(ay-by);
    int n; cin >> n;
    for(int i = 0; i < n; i++){
        int k, dir; cin >> k >> dir;
        
    }

    cout << ans << endl;
}
