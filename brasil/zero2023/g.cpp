#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(0); cin.tie(0)
#define int long long
#define endl "\n"

const int MAXN = 1e4+7;
int arr[MAXN], brr[MAXN];

signed main(){
    fastio;
    int n; cin >> n;
    //valor, vetor de indices 
    map<int, vector<int>> mp;
    for(int i = 0; i < n; i++){
        string s;
        getline(cin, s);
        for(int j = 0; j < s.size(); j++){
            //if(s[j] >= 'a' && str)
        }
    }

}