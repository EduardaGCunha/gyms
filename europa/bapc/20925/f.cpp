#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(0); cin.tie(0)
#define int long long
#define ll long long
#define dbg(x) cout << x << " "; 
#define endl '\n'



signed main(){
    fastio;
    string s;
    int n;
    cin >> s >> n;
    if(s == "send"){
        for(int i = 0; i < 30; i++){
            
        }
    }
    for(int i = 0; i < n; i++){

    }
}